#pragma once

namespace mmm
{
	namespace types
	{
		//Inherits from node, which inherits from database element.
		//Will need to be update - just used for member function calls
		//at the minute.
		struct ST3D_Camera
		{

		};

		ST3D_Camera* getCurrentCamera();
	}
}
